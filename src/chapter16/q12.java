package chapter16;
import java.util.*;
//given an xml code, write code to print the oncoded version of XML element
//family -> 1, person ->2, firstname -> 3, lastname -> 4, state -> 5
class Element{
    //this element is pseudo class, containing xml document
}
public class q12 {
    void encode(Element root, StringBuilder sb){
        //we would approach this problem using the tree method, given a root we proceed to 
        //the corresponding children, 
        // whenever we encounter new element we check if it match our set criteria and then
        //encode the whole process using the above methodoloogy by recursively calling the same 
        //function
    }
    public static void main(String[] args){}
}
