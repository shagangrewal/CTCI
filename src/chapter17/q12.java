package chapter17;
import java.util.*;
//given a datastructure, pointing to two other nodes, can be used to represent binary tree
//convert a binary search tree to doubly linked list using BiNode, elements in order
public class q12 {
    //pseudo-code for the above problem can be
    //BiNode a = convert(root.left)
    //BiNode b = convert(root.right)
    //mergeLists(a, root, b)
    //return the front end of the list

    public static void main(String[] args){}
}
