package chapter10;
//given an array like data-structure listy, lacks size() method
//has elementAt(i), return element at index(i)
//find the index at which an element x occurs, return any if multiple entries exit

public class q4 {
    
}
//int search(Listy list, int x){
  //  int index = 1;
    //while(list.elementAt(i) != -1 && list.elementAt(i)<x){
      //  index *= 2; 
    //}
    //return bSearch(list, index/2, index, x);
//}
 //int bSearch(List list, int low, int high, int x){
   // if(low>high)
     //   return -1;

//    int mid = (low+high)/2;
  //  int midE = list.elementAt(mid);
    //if(midE < x || midE = -1){
      //  high = mid-1;
    //}
    //else if(midE < x){
      //  low = mid+1;
    //}
    //else{
      //  return mid;
    //}
//return -1;
//}